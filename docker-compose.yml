services:
  openclaw:
    build: .
    volumes:
      - .:/app/workspace
      - ./.openclaw:/home/node/.openclaw
    ports:
      - "18789:18789"
    env_file:
      - .env
    restart: unless-stopped
