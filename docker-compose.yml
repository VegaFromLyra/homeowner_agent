services:
  openclaw:
    build: .
    volumes:
      - .:/app/workspace
      - ./.openclaw:/home/node/.openclaw
      - ./skills:/app/skills
    ports:
      - "18789:18789"
    env_file:
      - .env
    restart: unless-stopped
